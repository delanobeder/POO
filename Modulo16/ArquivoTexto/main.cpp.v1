#include <iostream>
using namespace std;

int main(int argc, char** argv) {

    int a, b, c, maior;
    
    cin >> a >> b >> c;
    
    if (a > b) {
        maior = a;
    } else {
        maior = b;
    }
    
    if (c > maior) {
        maior = c;
    }
    
    cout << "Maior valor e: " << maior << endl;
    return 0;
}

